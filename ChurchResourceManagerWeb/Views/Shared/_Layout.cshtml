<!DOCTYPE html>
<html lang="en" id="ResourceManagerApp" ng-app="app" ng-cloak>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    <script type="text/ng-template" id="message-template">
        <div ng-message="minlength">Message must be over 10 characters</div>
        <span ng-message="required">This is required!</span>
        <div ng-message="maxlength">Message must not exceed 100 characters</div>
        <div ng-message="equalWithHoh">Only one Head of Household is allowed</div>
        <div ng-message="equalWithSpouse">Only one Spouse is allowed</div>
    </script>


    <style>
        body {
            margin-top: 40px;
        }
    </style>

</head>
<body ng-cloak>
    @*<div class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    @Html.ActionLink("Church Resource Manager", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                </div>
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                        <li>@Html.ActionLink("Home", "Index", "Home")</li>
                        <li class="dropdown">
                            <a class="dropdown-toggle" data-toggle="dropdown" href="">Tithing <span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li>@Html.ActionLink("Add Tithes", "AddTithes", "Tithing")</li>
                            </ul>
                        </li>
                        <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                    </ul>
                    @Html.Partial("_LoginPartial")
                </div>
            </div>
        </div>
        <div id="mainContainer" name="mainContainer" class="container body-content">
            @RenderBody()
            <hr />
            <footer>
                <p>&copy; @DateTime.Now.Year - Church Resource Manager</p>
            </footer>
        </div>*@
    <div id="wrapper">
        <!-- Nav Bar -->
        @Html.Partial("_NavBar")

        <div id="page-wrapper">
            @*<div class="row">
                <div class="col-lg-12">*@
                    @RenderBody()
                @*</div>
            </div>*@
        </div>

    </div>


    @Scripts.Render("~/bundles/angular")
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/ChurchResourceManagerApp")
    @Scripts.Render("~/bundles/notifications")
    @RenderSection("scripts", required: false)
</body>
</html>
