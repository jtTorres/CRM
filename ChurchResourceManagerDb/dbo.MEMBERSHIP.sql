CREATE TABLE [dbo].[MEMBERSHIP]
(
	FAMILY_ID INT NOT NULL,
	MEMBER_ID INT NOT NULL IDENTITY(1,1),
	FIRST_NAME VARCHAR(80) NOT NULL,
	MIDDLE_NAME CHAR(1) NULL,
	LAST_NAME VARCHAR(90) NOT NULL,
	DOB DATE NULL,
	GENDER CHAR(1) NULL,
	LOCATION_ID SMALLINT NOT NULL,
	MARITAL_STATUS_ID TINYINT NOT NULL,
	GROUP_ID TINYINT NOT NULL,
	PREFERRERD_CONTACT_METHOD TINYINT NOT NULL,
	EXIT_DATE DATE NULL,
	MEMBERSHIP_TYPE_ID TINYINT NOT NULL,
	LAST_MODIFIED_DATE DATETIME NOT NULL,
	
	CONSTRAINT [pk_MEMBERSHIP_MEMBER_ID] PRIMARY KEY CLUSTERED (MEMBER_ID)
);
GO

ALTER TABLE dbo.MEMBERSHIP
WITH CHECK ADD CONSTRAINT [fk_MEMBERSHIP_to_FAMILIES_FAMILY_ID] FOREIGN KEY (FAMILY_ID)
REFERENCES dbo.FAMILIES (FAMILY_ID);
GO

ALTER TABLE dbo.MEMBERSHIP
WITH CHECK ADD CONSTRAINT [fk_MEMBERSHIP_to_LOCATIONS_LOCATION_ID] FOREIGN KEY (LOCATION_ID)
REFERENCES dbo.LOCATIONS (LOCATION_ID);
GO

ALTER TABLE dbo.MEMBERSHIP
WITH CHECK ADD CONSTRAINT [fk_MEMBERSHIP_to_MARITAL_STATUS_MARITAL_STATUS_ID] FOREIGN KEY (MARITAL_STATUS_ID)
REFERENCES dbo.MARITAL_STATUS (MARITAL_STATUS_ID);
GO

ALTER TABLE dbo.MEMBERSHIP
WITH CHECK ADD CONSTRAINT [fk_MEMBERSHIP_to_MEMBER_GROUPS_GROUP_ID] FOREIGN KEY (GROUP_ID)
REFERENCES dbo.MEMBER_GROUPS (GROUP_ID);
GO

ALTER TABLE dbo.MEMBERSHIP
WITH CHECK ADD CONSTRAINT [fk_MEMBERSHIP_to_MEMBERSHIP_TYPES_MEMBERSHIP_TYPE_ID] FOREIGN KEY (MEMBERSHIP_TYPE_ID)
REFERENCES dbo.MEMBERSHIP_TYPES (MEMBERSHIP_TYPE_ID);
GO

ALTER TABLE dbo.MEMBERSHIP
ADD CONSTRAINT [df_LAST_MODIFIED_DATE_CURRENT_DATE] DEFAULT GETDATE() FOR LAST_MODIFIED_DATE;
GO